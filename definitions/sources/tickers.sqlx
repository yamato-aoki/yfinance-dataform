config {
  type: "declaration",
  schema: "yfinance_analytics",
  name: "tickers"
}

SELECT
  ticker_id,
  company_name,
  sector_id,
  currency_id,
  country_id,
  is_index
FROM
  `yfinance_analytics.tickers`
